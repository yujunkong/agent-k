{
  "name": "agent-k-monorepo",
  "private": true,
  "version": "3.0.0-skeleton",
  "description": "Agent-K v3.0 clean transplant monorepo (skeleton only)",
  "workspaces": [
    "packages/*",
    "extensions/*"
  ],
  "scripts": {
    "check": "npm run test",
    "test": "npm run test -w @agent-k/shared && npm run test -w @agent-k/host",
    "typecheck": "npm run typecheck -w @agent-k/shared && npm run typecheck -w @agent-k/host && npm run typecheck -w agent-k"
  },
  "engines": {
    "node": ">=18"
  }
}
