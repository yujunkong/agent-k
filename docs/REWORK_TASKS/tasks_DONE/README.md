# tasks_DONE — 검증된 완료만

이 폴더에 있는 ID만 **코드 감사 PASS**로 인정합니다.

그 외 `REWORK_TASKS/tasks/*-R2.json` 또는 pending ID를  
“이미 했다 / compile 됐다 / 파일이 있다”고 **완료 처리하지 마세요.**

자세한 착각 사례: `../AGENT_HANDOFF.md` 의 **완료 착각 금지** 절, 각 R2의 `completionTrap` 필드.
