/**
 * EXT-002 / CHAT-001 — Shell is now the ChatApp layout.
 * Kept as a named export so older tests/imports keep working.
 */

export { ChatApp as Shell } from './ChatApp';
export { ChatApp } from './ChatApp';
