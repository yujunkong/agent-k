# @agent-k/chat-ui (stub)

**Status:** ownership stub — sources still in `src/chat/**` (+ settings React panels).

When editing chat UI, stay in those paths. Do not change `src/host/**` or tool executors
unless the task is explicitly cross-cutting (then use `protocol.ts`).

See `docs/architecture/MONOREPO_SPLIT.md` Phase 3 (extract last).
