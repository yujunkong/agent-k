# @agent-k/chat-ui (stub)

**Owns (today):** `src/chat/**`, settings React panels  
**Feature IDs:** CHAT-*, CONV-*, UI-*, CURSOR-*, STREAM-*(표시), SET-*(UI)  
**금지:** `vscode`, fs, agent loop, provider network  
Extract last (Phase 3) after protocol/shared edges are clean.
