{
  "name": "@agent-k/chat-ui",
  "version": "0.0.0",
  "private": true,
  "description": "Agent-K chat webview (React). Source still under src/chat until Phase 3 extract.",
  "agentk": {
    "owns": ["src/chat/**", "src/settings/**"],
    "status": "stub",
    "phase": 3,
    "bundler": "vite",
    "featureIds": ["CHAT-*", "CONV-*", "UI-*", "CURSOR-*", "SET-*(UI)", "STREAM-*(UI)"]
  }
}
