# @agent-k/shared

Pure types and protocol only. All packages may depend on shared; shared depends on none of them.

Phase 0 extract target for host↔webview message types and Typed Work Events (R-002).
