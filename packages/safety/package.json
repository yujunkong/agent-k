{
  "name": "@agent-k/safety",
  "version": "0.0.0",
  "private": true,
  "description": "Permission, secrets, checkpoint, hooks, verification (2nd-wave).",
  "agentk": {
    "owns": [
      "src/permission/**",
      "src/secrets/**",
      "src/checkpoint/**",
      "src/hooks/**",
      "src/verification/**"
    ],
    "status": "stub",
    "phase": 2,
    "featureIds": ["SAFE-*"]
  }
}
