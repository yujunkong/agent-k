# @agent-k/safety (stub · 2차)

**Owns (today):** permission, secrets, checkpoint, hooks, verification under `src/`  
**Feature IDs:** SAFE-*  
Until extract, edits may land via `core`/`tools` ownership — still follow deny/hook contracts in the rule.
